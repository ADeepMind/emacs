#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int compare(int a, int b) {
  return a > b;
}

int main (void) {
  // int arr[5] = {7,1,3,6,2};
  // sort(arr, arr + 5, compare);
  // for (int i = 0; i < 5; ++i)
  //   cout << arr[i] << " ";
  // cout << endl;
  vector<int> a;
  cout << a.size() - 1 << endl;
}
