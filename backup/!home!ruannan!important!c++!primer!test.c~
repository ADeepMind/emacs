#include <stdio.h>

struct A {
  int a;
  int b;
};

int main(void) {
  unsigned long sum = 0;
  for (int i = 1; i <= 100000; ++i) {
    for (int j = 1; j <= 100000; ++j) {
      sum += i * j;
    }
  }
  printf("end\n");
  // struct A c;
  // printf("%d, %d, %p\n", c.a, c.b, &c.a);
  return 0;
}
